using UnityEngine;


// Holds data about the shot and tracks if the ball has already scored.

public class BallStatus : MonoBehaviour
{
    public ShotType shotType;
    public bool hasScored = false;
}
